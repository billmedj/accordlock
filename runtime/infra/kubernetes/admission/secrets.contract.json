{
  "schemaVersion": 1,
  "namespace": "accordlock-system",
  "requiredSecrets": [
    {
      "name": "accordlock-webhook-server-tls",
      "requiredKeys": [
        "tls.crt",
        "tls.key"
      ],
      "purpose": "HTTPS server certificate and private key for accordlock-webhook.accordlock-system.svc"
    },
    {
      "name": "accordlock-postgres-auth",
      "requiredKeys": [
        "password"
      ],
      "purpose": "PostgreSQL password mounted as a file"
    },
    {
      "name": "accordlock-postgres-ca",
      "requiredKeys": [
        "ca.crt"
      ],
      "purpose": "Trust anchor used to authenticate the PostgreSQL TLS server"
    }
  ],
  "optionalSecrets": [
    {
      "name": "accordlock-postgres-client-tls",
      "requiredKeys": [
        "tls.crt",
        "tls.key"
      ],
      "purpose": "Optional PostgreSQL client certificate and key; deliberately not mounted by the base candidate"
    }
  ]
}
