# Deliberately inert. AccordLock injects its signed release feed at build time and keeps
# updates disabled until that feed exists. This file must never point at upstream Goose.
provider: github
owner: updates.invalid
repo: updates-disabled
updaterCacheDirName: accordlock-updater
